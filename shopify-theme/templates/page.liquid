{% comment %}
  Default page template
{% endcomment %}

<div class="page-width">
  <header class="section-header">
    <h1>{{ page.title }}</h1>
  </header>
  
  <div class="rte">
    {{ page.content }}
  </div>
</div>
